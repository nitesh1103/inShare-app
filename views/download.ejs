<section class="download-section">
  <div class="download-container">
    <img src="/img/download-sd.svg" class="download-icon" alt="download-img">
    <% if( locals.error ) { %>
      <h4><%= locals.error %> 😏</h4>
    <% } else { %>
      <h2>Your file is ready to download</h2>
      <p>Link expires in 24 Hours</p>
      <h3><%= locals.fileName %></h3>
      <h4><%= parseInt(locals.fileSize / 1000) %> KB</h4>
      <div class="download-btn">
        <a href="<%= locals.downloadLink %>">Download File</a>
      </div>
    <% } %>
  </div>
</section>

